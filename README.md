# Deployable Static Site Generated With Clojure

## Setup

### Local instance

Set `REMOTE_USER`, `REMOTE_HOST`, `REMOTE_PATH` in your `.env` file. `REMOTE_PATH` is the path to this repo on the remote instance.

### Remote instance

Set `STATIC_SITE_PATH` in the `.env` file. This is where the built site will be placed.